{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "build/src/server.js",
  "scripts": {
    "build": "tsoa spec-and-routes && tsc --outDir build",
    "start": "node build/src/server.js",
    "test": "cross-env NODE_ENV=test jest --forceExit --verbose --runInBand --collectCoverage=true ./tests/unit",
    "test-integration": "cross-env NODE_ENV=test jest --forceExit --verbose --runInBand --collectCoverage=true ./tests/integration"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.41",
    "@types/supertest": "^2.0.12",
    "cross-env": "^7.0.3",
    "husky": "^6.0.0",
    "jest": "^26.6.3",
    "jest-extended": "^0.11.5",
    "lint-staged": "^11.2.6",
    "prettier": "2.3.0",
    "supertest": "^6.1.6",
    "ts-jest": "^26.5.0",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "express": "^4.18.1",
    "tsoa": "^4.1.0"
  }
}
